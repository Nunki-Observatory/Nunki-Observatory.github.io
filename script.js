// Add interactivity for forms or dynamic content if needed
document.querySelector('form').addEventListener('submit', function(event) {
    event.preventDefault();
    alert('Mesajul tău a fost trimis!');
});
